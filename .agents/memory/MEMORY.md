- [Platform audit fixes](platform-audit-fixes.md) — comprehensive fix log for fake data, broken links, auth, onboarding, nav, N+1 queries, and shared utilities
- [Express route prefix](express-route-prefix.md) — `app.use("/api", router)` strips `/api`; routes in the router must NOT include `/api/` prefix.
- [SSE auth pattern](sse-auth.md) — EventSource can't set headers; accept auth token via `?token=` query param for SSE endpoints.
- [Admin seed endpoint](admin-seed.md) — `POST /api/setup/seed` provisions 6 accounts: CEO(Leonardo), CMO(Jean), CCO(Qaialla `Qaialla27@`), CEO Master, 2 test. No ecosystem data.
- [Multi-Replit & governance docs](multi-replit-governance.md) — 4 master accounts: Leonardo(CEO), Jean Dick(CMO), Qaialla Pereira(CCO `qaialla.exclusive@gmail.com`), extrago.ceo. Profile assets: team-leonardo.jpg, team-jean.jpg, team-qaialla.jpg.
- [Progression & referral tiers](progression-engine.md) — 5 level tiers (fees 20/18/15/12/10) + 3 referral tiers (2/3/5%); internal level keys ≠ labels (diamond=Elite). Switches on level MUST cover all 5 keys.
- [Admin pages](admin-pages.md) — Admin now has 9 pages: dashboard, users, jobs, withdrawals, analytics, ops, map, representatives, governance. Chart library: CSS-only bars (no recharts).
- [CEO Governance Center](ceo-governance.md) — 7 tabs: Config, Financeiro, Categorias, Carteira, Usuários, Equipe, Badges. Split engine wires into completeJobCascade via loadSplitConfig() (loaded BEFORE the DB tx). Financial tab: split rates, escrow rules, withdrawal rules, Asaas read-only status. Categories CRUD (soft-archive). Carteira: platform wallet metrics + paginated ledger. Badge grants stored as `badge:{userId}:{name}` in platformConfigTable.
- [Split Engine & Financial Ledger](split-engine-ledger.md) — lib/split-engine.ts: loadSplitConfig()/calculateSplit()/calculateReferralRate()/invalidateSplitConfigCache(); 60s in-memory cache reads platformConfigTable keys `financial.*`. New DB tables: categoriesTable, walletLedgerTable, escrowsTable (all exported from lib/db/src/schema/index.ts). Governance route at artifacts/api-server/src/routes/governance.ts owns all `/admin/governance/*` endpoints.
- [Production data enforcement](production-data.md) — DB has 6 approved accounts: IDs 1(CEO), 16(CMO), 30(CCO), 5(CEO Master), 6(test-F), 7(test-E). seed.ts ONLY provisions these — never ecosystem/demo data. Always re-run seed after password changes.
- [Deposit flow](deposit-flow.md) — Company deposit uses useRequestDeposit hook (POST /wallet/deposit-request). Admin uses 2-step confirm→approve flow via useAdminConfirmDepositRequest({id}) and useAdminApproveDepositRequest({id,data?}). Confirm only takes {id} — no data property.
- [Generated type gaps](generated-type-gaps.md) — AdminStats has no jobsByStatus field; ReferralInfo has no activeReferrals (use totalConverted); Job has no city field. Always check openapi.yaml before using schema properties.
- [Unified navigation](unified-navigation.md) — ONE navbar (InstitutionalNavbar=UnifiedNavbar) for public+auth; sidebar/bottom-tabs/Mais-sheet/avatar-dropdown REMOVED. nav-config.tsx = drawer source of truth, data-driven locks. Gotcha: admin in unlocked[] must match ProtectedRoute allowedRoles or links dead-bounce.
- [Form crash gotcha](unified-navigation.md) — FormLabel outside FormField context causes runtime crash — use plain `<label>` instead.
- [App background system](app-background.md) — Authenticated area uses AppBackground (position:fixed, z-index:0) with /app-background.png. App.tsx wrapper div must NOT have bg-background class (removed) or it blocks the image. Body bg-background in index.css is kept as fallback only. Sidebar: bg rgba(2,5,10,0.72) backdrop-blur. Sidebar simplified to 5 items max per role.
- [Terminology: Extra not Vaga](terminology-extra.md) — All user-visible "Vaga/Vagas/Job/Jobs" replaced with "Extra/Extras" across the entire UI. "vagas" meaning slot-count (workersNeeded) is kept. Authenticated header uses navbarBg image (no logoMain) and PAGE_TITLES map.
- [Level badge system](level-badge-system.md) — Sprite-based badges in `components/level-badge.tsx`. Uses CSS background-position sprite technique from `/badges/freelancer-badges.png`, `/badges/indicacoes-badges.png`, `/badges/corporate-badges.png`. Exports LevelBadge, LevelBadgeIcon, ReferralBadge, ReferralBadgeIcon, CorporateBadge, CorporateBadgeIcon.
- [Navbar public mode](navbar-public-mode.md) — UnifiedNavbar uses `effectiveUser = isInstitutionalPage ? null : user` so public pages always show public nav even when logged in. INSTITUTIONAL_PATHS defined inline in the component. Home button always goes to `/`.
- [Investor page](investor-page.md) — Public route `/investidores-parceiros` at `pages/investidores-parceiros.tsx`. Executive photos served from `/public/` as `team-leonardo.jpg`, `team-jean.jpg`, `team-qaialla.jpg`. Banner image at `/public/investors-banner.png`. Added to landing navbar, all account menus (admin/company/freelancer), and MaisNavSheet for all roles.
- [Governance avatar pipeline](governance-avatars.md) — avatarUrl stored as text in DB (Base64 or public path). Governance identities use static public paths (/team-leonardo.jpg, /team-jean.jpg). Set via seed.ts upsertUser; call POST /api/setup/seed after any photo change to push to DB. Shared ReferralSimulator at components/referral-simulator.tsx (variant: landing|app) — landing.tsx and referrals.tsx both import from here.
- [Auth/KYC/Legal Phase 1 foundation](auth-verification-phase1.md) — account-status machine, shared email_verifications table, delivery stub, drizzle-push TTY workaround.
- [Institutional pages overlay rules](institutional-overlay-rules.md) — VISUAL_GUIDELINES: overlay max 0.55, textShadow max rgba(0,0,0,0.42), depth vignette max rgba(0,0,0,0.38). Content Surface: landing sections use light radial `rgba(255,255,255,0.022-0.028)` (slightly lighter); investor sections use `rgba(4,10,22,0.08-0.10)` background (slightly darker). No cards, no added wrappers.
- [Phase 3 execution engine](phase3-execution.md) — job_events + job_codes DB tables, 9-status machine, 6-digit checkin/checkout codes, wallet reservation on job create, daily shift type (7h20).
